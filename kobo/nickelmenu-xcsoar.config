# NickelMenu entries for KOBO_NICKEL / Clara BW (P365).
# Copy to KOBOeReader/.adds/nm/config (merge with existing lines).
# Requires NickelMenu: https://pgaskin.net/NickelMenu/
#
# Do not kill nickel before launch — MTK Kobos reboot if nickel exits.

menu_item :main :XCSoar :cmd_spawn :quiet:/mnt/onboard/XCSoarData/kobo/run_xcsoar.sh -simulator
menu_item :main :XCSoar (fly) :cmd_spawn :quiet:/mnt/onboard/XCSoarData/kobo/run_xcsoar.sh -fly
